[package]
name = "bartok-settlement"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
miden = { version = "0.12" }

[package.metadata.component]
package = "miden:bartok-settlement"

[package.metadata.miden]
project-kind = "note-script"
