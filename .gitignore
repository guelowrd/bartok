# secrets — never commit
.env
**/.env

# TLSNotary checkout (re-created by zktls-spike/setup.sh)
zktls-spike/tlsn/

# build + notarization artifacts
**/target/
**/*.tlsn

# os
.DS_Store

# miden client state (testnet keystore + sqlite store) — never commit keys
miden/keystore/
miden/store.sqlite3*
miden/accounts.json

# notary/oracle keys
keys/

# ux-prototype build
ux-prototype/node_modules/
ux-prototype/dist/

# compiled note package for the browser (regenerated by: npm run build:contracts)
ux-prototype/public/packages/
