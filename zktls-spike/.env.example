# M2 (real-model zkTLS spike) needs a free OpenRouter key.
# Create one at https://openrouter.ai/keys  (no card required; use a ":free" model = $0, no pay-per-token)
# Then copy this file to `.env` and paste your key:
OPENROUTER_API_KEY=
