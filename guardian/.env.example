# Bartok-Guardian overrides (copy to .env; everything has sane defaults in run.sh)
# BARTOK_GUARDIAN_SRC=$HOME/Code/bartok-guardian
# GUARDIAN_NETWORK_TYPE=MidenTestnet
# Optional at-rest encryption for guardian state (dev key source):
# GUARDIAN_STORAGE_ENCRYPTION_KEY=$(openssl rand -base64 32)
