{
  "name": "bartok-ux",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:contracts": "cd ../miden/contracts/settlement-note && cargo miden build --release && mkdir -p ../../../ux-prototype/public/packages && cp target/miden/release/bartok*settlement.masp ../../../ux-prototype/public/packages/bartok-settlement.masp",
    "test": "node --test test/"
  },
  "dependencies": {
    "@miden-sdk/miden-sdk": "0.15.3",
    "@openzeppelin/miden-multisig-client": "file:./vendor/miden-multisig-client",
    "@openzeppelin/guardian-client": "file:./vendor/guardian-client"
  },
  "devDependencies": {
    "@miden-sdk/vite-plugin": "0.15.3",
    "vite": "^6.0.0"
  },
  "overrides": {
    "@miden-sdk/miden-sdk": "0.15.3"
  },
  "engines": {
    "node": ">=20"
  }
}